import 'package:flutter/material.dart';

class BiasAnalysisScreen extends StatelessWidget {
  const BiasAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SatyaPress - Layer 2: Bias Analysis'),
      ),
      body: const Center(
        child: Text('Bias Classification & Perspectives (Placeholder)'),
      ),
    );
  }
}
